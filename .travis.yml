matrix:
  include:
    - language: cpp
      addons:
        apt:
          packages:
            - libxerces-c-dev
            
      script:
        - mkdir regxmllib/build
        - cd regxmllib/build
        - cmake ..
        - cmake --build .
        - ctest .
      
    - language: java
      install:
        - sudo apt-get install junit
      script:
        - cd regxmllib
        - ant test -lib /usr/share/java